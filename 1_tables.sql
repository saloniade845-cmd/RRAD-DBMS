USE rrad_db;
SELECT *
FROM `rajasthan rainfall analytics database (rrad)`
LIMIT 10;
SELECT COUNT(*) AS total_rows
FROM `rajasthan rainfall analytics database (rrad)`;
DESCRIBE `rajasthan rainfall analytics database (rrad)`;
SELECT *
FROM `rajasthan rainfall analytics database (rrad)`;
